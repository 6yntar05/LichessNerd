#include "chess/chess.hxx"
#include "utils/token.hxx"
#include "ui/ui.hxx"
#include <iostream>

int main(int argc, char** argv) {
    //utils::parse(argc, argv);
    
    //chess::board board;

    //std::string token = utils::readToken("./token.txt");
    //chess::api::account account = chess::api::getAccount(token);
    //auto gameStream = chess::asyncStream(account.latestGame);
    
    //auto movesHistory = gameStream.pull();
    //board.init(movesHistory);

    //gameStream.setCallBack(board.asyncCallBack);

    //gameStream.terminate();
    return 0;
}